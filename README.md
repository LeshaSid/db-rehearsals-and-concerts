# 🎵 Система управления студией (Rehearsals and Concerts Management)

## 📌 Обзор проекта

"Система управления студией" — это веб-приложение, разработанное на **Streamlit** и **PostgreSQL**, предназначенное для комплексного управления ключевыми ресурсами и событиями музыкальной студии.

Приложение обеспечивает учет, планирование и аналитику для следующих сущностей:
* **Музыканты**.
* **Коллективы** (Группы).
* **Репетиции**.
* **Концерты**.

---

## ✨ Ключевые функции

### 🏠 Главная страница (`main.py`)
* **Метрики и Статистика:** Отображает общее количество музыкантов, коллективов, концертов и репетиций.
* **Ближайшие События:** Выводит список всех запланированных репетиций и концертов на выбранный период (от 1 до 30 дней вперёд) с указанием даты, времени, места и типа события.

### 🎸 Управление Коллективами (`bands.py`)
* **Список:** Отображает список групп с указанием жанра, даты основания, общего числа участников и количества проведенных репетиций.
* **CRUD-операции:** Предоставляет форму для **Создания** новых коллективов и **Редактирования/Удаления** существующих (включая каскадное удаление связанных репетиций, концертов и членства).
* **Управление Составом:** Позволяет добавлять **доступных** музыкантов в группу и удалять текущих участников через отдельную вкладку.

### 🛠️ Ядро и Константы (`rac_lib.py`)
* **База данных:** Содержит функции для подключения к PostgreSQL и выполнения запросов (`run_query`, `execute_non_query`, `delete_record`).
* **Справочники:** Определены константы для **Инструментов** (например, Гитара, Барабаны, Вокал) и **Жанров** (например, Рок, Джаз, Классика).
* **Навигация:** Формирует боковое меню для перехода между страницами Streamlit.

---

## 💻 Технологический стек и Зависимости

Проект построен на Python с использованием следующих ключевых инструментов и библиотек:

| Категория | Технология | Версия | Назначение |
| :--- | :--- | :--- | :--- |
| **Приложение** | Streamlit | `>=1.51.0` | Веб-интерфейс. |
| **База данных** | PostgreSQL | (Внешняя) | Хранение данных. |
| **Драйвер DB** | `psycopg2` | `>=2.9.11` | Подключение к PostgreSQL. |
| **Аналитика** | `pandas` | `>=2.3.3` | Обработка данных. |
| **Визуализация** | `matplotlib`, `plotly` | `>=3.10.7`, `>=6.5.0` | Построение отчетов и графиков (для раздела "Отчеты"). |

### Управление зависимостями с помощью `uv`

Проект использует `pyproject.toml` для декларации зависимостей. Для управления средой и установки пакетов рекомендуется использовать быстрый менеджер пакетов **`uv`**.

---

## ⚙️ Инструкция по установке и запуску

### 1. Подготовка среды

1.  Установите Python 3.13 или выше.
2.  Установите `uv` (если еще не установлен):
    ```bash
    pip install uv
    ```
3.  Установите зависимости проекта из `pyproject.toml`:
    ```bash
    uv pip install -r requirements.txt # Или укажите все пакеты вручную, если requirements.txt отсутствует:
    # uv pip install matplotlib>=3.10.7 pandas>=2.3.3 plotly>=6.5.0 psycopg2>=2.9.11 streamlit>=1.51.0
    ```

### 2. Настройка базы данных PostgreSQL

Приложение ожидает, что база данных PostgreSQL будет доступна.

1.  Убедитесь, что сервер PostgreSQL запущен.
2.  **Проверьте/Создайте БД:** В файле `rac_lib.py` заданы следующие параметры подключения:
    * `host="localhost"`
    * `database="concerts and rehearsals"`
    * `user="postgres"`
    * `password=""` (пустая строка)
    * `port=5432`

    Если эти параметры не совпадают с вашей конфигурацией, **обязательно отредактируйте** функцию `init_connection()` в `rac_lib.py`.

3.  **Создайте таблицы:** Необходимо создать соответствующие таблицы (`musicians`, `bands`, `rehearsals`, `concerts`, `band_membership`, `performances`) и их схемы, прежде чем запускать приложение.

### 3. Запуск приложения

Запустите Streamlit-приложение из корневой директории проекта:

```bash
streamlit run main.py
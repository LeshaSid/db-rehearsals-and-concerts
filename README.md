# 🎵 Система управления студией (Rehearsals and Concerts Management)

## 🌟 Обзор проекта

**Система управления студией** — это полнофункциональное веб-приложение, разработанное на **Streamlit** и работающее на базе **PostgreSQL**. Оно предназначено для комплексного учета и планирования всех процессов музыкальной студии: от управления музыкантами и коллективами до бронирования репетиций, организации концертов и создания аналитических отчетов.

### 💻 Технологический стек

| Компонент | Технология | Версия (мин.) | Назначение |
| :--- | :--- | :--- | :--- |
| **Фронтенд/Приложение** | Streamlit | `>=1.51.0` | Быстрая разработка интерактивного веб-интерфейса. |
| **База данных** | PostgreSQL | — | Надёжное хранение данных и поддержка транзакций. |
| **Драйвер БД** | `psycopg2` | `>=2.9.11` | Соединение Python с PostgreSQL. |
| **Управление пакетами** | **`uv`** | — | Ультрабыстрая установка и управление зависимостями. |

---

## ✨ Детальный функционал по страницам

Проект использует многостраничную структуру Streamlit, а навигация определяется в `rac_lib.py`.

### 1. 🏠 Главная (`main.py`)
* **Сводная статистика:** Ключевые метрики (Музыканты, Коллективы, Концерты, Репетиции).
* **Ближайшие события:** Список Концертов (🎭) и Репетиций (🎻) на период до 30 дней.
* **Производительность:** Статистические данные и ближайшие события кэшируются на **$5$ секунд** (`@st.cache_data(ttl=5)`) для ускорения загрузки.

### 2. 🎵 Музыканты (`musicans.py`)
* **Валидация данных:** При создании нового музыканта выполняется **проверка формата телефона** (`+375XXXXXXXXX`).
* **Управление (CRUD):** Просмотр, поиск, добавление, редактирование и удаление.
* **Каскадное удаление:** Удаление музыканта автоматически исключает его из всех коллективов (запись удаляется из `band_membership`).

### 3. 🎸 Коллективы (`bands.py`)
* **Создание/Удаление:** Регистрация групп с выбором **Жанра** из справочника. При удалении происходит **каскадное удаление** связанных репетиций, выступлений (`performances`) и членства.
* **Управление Составом:** Добавление/удаление участников. Система предлагает к добавлению только тех музыкантов, которые **не состоят** в текущем коллективе.

### 4. 🎻 Репетиции (`rehearsals.py`)
* **Планирование:** Бронирование залов (`LOCATIONS`) с указанием Коллектива, Даты, Времени и Длительности (`DURATIONS`).
* **Контроль конфликтов:** Приложение **проверяет пересечение** по времени и месту при бронировании, исключая двойное резервирование зала.
* **Расписание:** Просмотр списка будущих репетиций (до 90 дней) с возможностью фильтрации.

### 5. 🎭 Концерты (`concerts.py`)
* **Создание и состав:** Сложная форма для регистрации концертов и определения **порядка выступления** участвующих коллективов (записывается в таблицу `performances`).
* **Обзор:** Список концертов с указанием количества групп и их перечислением (используется `STRING_AGG` в SQL).
* **Массовое удаление:** Возможность удалить несколько концертов одновременно, с предварительным удалением связанных выступлений.

### 6. 📊 Отчеты (`reports.py`)
Страница аналитики с использованием **Plotly** для визуализации данных:
* **Активность репетиций:** Топ-10 коллективов по **суммарным часам** репетиций за настраиваемый период.
* **Свободные музыканты:** Список музыкантов, которые **не участвуют** ни в одном коллективе (`LEFT JOIN ... WHERE IS NULL`).
* **Распределение по жанрам:** Анализ долей коллективов по Жанрам в виде диаграммы.

---

## 🔑 Ключевая структура данных и константы

### Схема таблиц PostgreSQL

Для корректной работы приложения требуется следующая минимальная схема таблиц с установленными связями (FK):

| Таблица | Описание | Ключевые связи (FK) |
| :--- | :--- | :--- |
| `musicians` | Каталог музыкантов. | — |
| `bands` | Каталог коллективов. | — |
| `concerts` | Информация о концертных площадках и датах. | — |
| `rehearsals` | Планирование репетиций. | `band_id` (FK) |
| `band_membership` | Связь Музыкант $\leftrightarrow$ Коллектив. | `musician_id` (FK), `band_id` (FK) |
| `performances` | Состав концерта и порядок выступлений. | `concert_id` (FK), `band_id` (FK) |

### Справочники (`rac_lib.py`)
Ключевые константы, используемые в приложении:
* `LOCATIONS`: Список доступных помещений для репетиций.
* `INSTRUMENTS`: Словарь для унификации названий инструментов (рус. $\leftrightarrow$ англ. код).
* `GENRES`: Словарь для унификации названий музыкальных жанров (рус. $\leftrightarrow$ англ. код).

---

## 🚀 ГАЙД ПО УСТАНОВКЕ И ЗАПУСКУ (С `uv`)

Этот раздел описывает процесс развертывания проекта с использованием **`uv`** — быстрого менеджера пакетов, написанного на Rust.

### 1. Подготовка среды

1.  Установите **Python $\ge 3.13$** и **PostgreSQL Server**.
2.  Установите менеджер пакетов **`uv`**:
    ```bash
    pip install uv
    ```

### 2. Установка зависимостей с помощью `uv`

Проект использует `pyproject.toml` для декларации всех зависимостей.

1.  Перейдите в корневую директорию проекта.
2.  Выполните установку:
    ```bash
    # uv автоматически создает виртуальное окружение .venv (если оно не существует)
    # и устанавливает все зависимости из pyproject.toml.
    uv sync
    ```
    > ⚡️ **Преимущество `uv`:** `uv` выполняет разрешение зависимостей и установку в 10-100 раз быстрее, чем традиционный `pip`, что обеспечивает молниеносный старт проекта.

### 3. Конфигурация PostgreSQL

1.  Убедитесь, что ваш сервер PostgreSQL запущен.
2.  **Создайте базу данных:** Используя клиент PostgreSQL, создайте базу данных с точным именем: **`concerts and rehearsals`**.

3.  **Создайте схему таблиц:** Создайте все шесть таблиц, перечисленных в разделе "Схема таблиц PostgreSQL", включая первичные и внешние ключи.

4.  **Проверьте параметры подключения:** Убедитесь, что функция `rac_lib.py::init_connection()` настроена правильно для вашего окружения:
    ```python
    # Проверьте host, user и password. По умолчанию:
    # host="localhost", database="concerts and rehearsals", user="postgres", password="", port=5432
    ```

### 4. Запуск приложения

1.  Активируйте виртуальное окружение (если вы не используете `uv run`):
    ```bash
    source .venv/bin/activate  # macOS/Linux
    .venv\Scripts\activate     # Windows
    ```
2.  Запустите Streamlit-приложение:
    ```bash
    streamlit run main.py
    ```

Приложение будет доступно в вашем браузере по адресу `http://localhost:8501`.